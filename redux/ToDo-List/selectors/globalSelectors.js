export const getText = (state) => state.changeTodos.message;
export const showTodos = (state) => state.filterTodos.show;
export const getTodo = (state) => state.changeTodos.data;
export const getFilter = (state) => state.filterTodos.filter;